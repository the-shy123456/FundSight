{
  "name": "app",
  "private": true,
  "version": "0.1.3",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "web:dev": "cross-env VITE_API_BASE=http://127.0.0.1:18080 npm --prefix ../../web run dev -- --host 127.0.0.1 --port 5173",
    "web:build": "cross-env VITE_API_BASE=http://127.0.0.1:18080 npm --prefix ../../web run build"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2"
  },
  "devDependencies": {
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "@vitejs/plugin-react": "^4.6.0",
    "typescript": "~5.8.3",
    "vite": "^7.0.4",
    "@tauri-apps/cli": "^2",
    "cross-env": "^7.0.3"
  }
}
